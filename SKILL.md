# 🌌 AI Neat Skill — AI 编年史记忆剪枝技能

> **Cross-platform Agent Skill** — Gemini CLI · Claude Code · OpenAI Codex 通用。
> 跨平台 SKILL.md，遵循开放 Agent Skill 规范。

这是一个用于大模型 CLI 工具（如 Gemini CLI、Claude Code）的无损记忆管理流。

## ⚙️ AI 运行激活协议 (System Prompt)

当用户输入 **“编年史”** 或 **“/ai-neat-skill”**，或者你在本项目环境中启动且上下文过长时，请严格遵守以下行为树：

1. **禁止直接抹除**：当上下文过长需要清理时，严禁直接丢弃历史对话。

2. **编年史压缩**：必须将过去 20 轮以上的碎片调试废话，按**事件**提炼为包含 [精确日期] 的「核心事件快照」（每个独立事件占用一行）。

3. **记忆追加**：将快照有序追加到 `memory.md` 中，随后清空原始冗余上下文，释放 Token 空间。

## 🛠️ 执行细节 (Execution Details)

### 第零步：尺寸体检 (Anti-Bloat Check)
在执行任何压缩动作前，检查 `memory.md` 的状态：
- **Soft Limit**: 若 `memory.md` 超过 200 行，考虑对更早期的历史事件进行二次精简或归档。
- **防止冗余**: 检查本次待追加的事件是否已在 `memory.md` 中存在，避免重复记录。

### 第一步：事件盘点 (Chronicle Inventory)
不要直接开始写，先在心中或内部输出本次会话的「待压缩清单」：
1. **架构/设计决策**：确定了哪些方案？
2. **功能增量**：完成了哪些特性？
3. **关键 Bug**：修复了什么，根因是什么？
4. **环境变更**：更新了哪些配置或依赖？

### 第二步：核心事件提炼 (Extraction)
按以下原则进行无损压缩：
- **绝对日期**：使用 `2026-05-17`，禁止使用“今天”、“昨天”。
- **原子化**：一个事件对应一行，描述必须清晰且具备独立可读性。
- **深层链接 (Deep Link)**：如果事件涉及复杂的逻辑变更，必须在快照中添加“指针”（例如：`详见 docs/auth.md`），引导未来的 AI 去读文档而不是猜测历史。
- **提炼格式**：`[YYYY-MM-DD] <分类>：<事件描述> (指针/关联路径)`

### 第三步：更新 memory.md (Sync) & 上下文交接 (Handover)
1. **记忆同步**：
   - 确保文件头部有清晰的项目描述。
   - 将新快照追加在 `---` 分割线之后。
   - **减优于加**：如果发现旧的快照已被后续事件覆盖，应直接修改或删除旧快照。
2. **上下文交接 (Context Handover)**：
   - **防断层**：在清理原始上下文前，必须识别当前是否有“正在进行中”的任务。
   - **交接条目**：在 `memory.md` 末尾临时追加一个 `[PENDING]` 标签的任务块，记录“下一步要做的 1-2 件事”，防止剪枝后 AI 丢失短期目标。

### 第四步：自检与确认 (Validation)
- [ ] 所有的快照是否都包含精确日期？
- [ ] `memory.md` 是否依然保持单行事件的简洁性？
- [ ] 是否已确认快照成功写入文件？
- [ ] 告知用户：“已将 X 个核心事件存入编年史，已释放约 Y 轮对话的 Token 空间。”

## 📂 资源引用 (References)
- **memory.md**：项目编年史记忆文件，存储核心事件快照。
